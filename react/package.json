{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@sentry/cli": "^1.37.0",
    "history": "^1.17.0",
    "material-ui": "^0.18.7",
    "react": "^15.6.2",
    "react-dom": "^15.6.2",
    "react-router": "^1.0.3",
    "react-scripts": "1.0.10",
    "request": "^2.88.0",
    "serve": "^10.1.1"
  },
  "scripts": {
    "predeploy": "npm run build && make setup_release",
    "deploy": "serve -s build",
    "deploylocal": "npm run buildlocal && make setup_release && serve -s build",
    "start": "REACT_APP_RELEASE=`sentry-cli releases propose-version` react-scripts start",
    "build": "REACT_APP_RELEASE=`sentry-cli releases propose-version` react-scripts build",
    "buildlocal": "REACT_APP_RELEASE=`sentry-cli releases propose-version` REACT_APP_BACKEND=http://localhost REACT_APP_PORT=3001 react-scripts build",
    "eject": "react-scripts eject",
    "deploy-ghp": "npm run predeploy && gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
